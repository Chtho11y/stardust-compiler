// pointer & address

func sum(x: int*, y: int*) -> int {
    *x = *x + 2;
    let res: int = *x + *y;
    res
}

func test_n1 () -> int{
    let a = 1;
    let b = a + 10;
    b = sum(&a, &b);
    0
}